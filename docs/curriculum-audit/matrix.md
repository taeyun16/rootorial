# Curriculum interaction audit matrix

This matrix separates source readiness from observed learner experience. `available`
means a renderer is registered; it does not count as browser-verified until the row
contains reproducible in-app-browser evidence in both desktop and mobile states.

Audit states: `unverified`, `in-progress`, `passed`, `failed`, `planned`.

Shared Transformer practice contract, revalidated 2026-07-31: all ten chapters
declare one reproduce, diagnose, and transfer challenge with direct choices,
adjacent expected/actual evidence, current/all reset, and optional
completion-gate separation. The shared footer now advances only after a pass
to the next incomplete challenge, supports Enter/Space, wraps around completed
items, restores focus to the learner workspace, and disappears at 3/3.
English Vectors 1280×720 and Korean Mini Transformer 390×844 passed with zero
console warnings/errors, native selects, page/practice overflow, or enabled
practice targets below 44px; mobile footer actions use the full 308px content
width.

Failure-feedback contract, Chrome-revalidated 2026-07-31: all 30 Transformer
challenges provide 90 localized explanations within the shared mobile reading
bounds. The shared result surface promotes and labels the original first
failed contract without hiding or reordering the remaining evidence. English
Vectors at 1280×720 and Korean Mini Transformer at 390×844 both reproduced a
late failure, promoted the original first failed item, and passed after one
targeted retry. Both had zero console warnings/errors, native selects,
document/practice overflow, or enabled practice targets below 44px. Unit/SSR,
build, curriculum-quality, and E2E discovery checks also pass. This is Chrome
evidence requested after the Codex in-app-browser webview failed to attach
across three consecutive runs; it is not represented as in-app-browser proof.

Registry-derived interaction inventory, updated 2026-08-02: all 32 implemented chapters
join cleanly across renderer, experience, content-quality, source, and declared
E2E contracts with zero structural issues. Browser-spec signals currently
cover route 32/32, desktop 32/32, 390×844 32/32, keyboard 32/32, immediate
result 32/32, console 25/32, overflow 32/32, 44px 32/32, and native-select-zero
29/32, leaving 10 explicit coverage targets. These are source-pattern
inventory signals rather than proof that every control was activated. CURR-049
adds the missing shared route spec and Codex in-app-browser evidence: all five
advanced Linux Networking routes opened at 1280×720 and 390×844, exposed
immediate wrong/correct prediction feedback, accepted Enter, unlocked the next
execution step, and had zero console errors, native selects, horizontal
overflow, or enabled core targets below 44px. The repository Playwright runner
discovers both tests but cannot execute them without the global Clerk
publishable key. The Interfaces chapter now also carries an explicit
native-select-zero assertion in both its desktop and 390×844 flows, completing
that signal across all six Linux Networking chapters.

| Curriculum | Chapter | Source | Audit | In-app browser evidence | Learning output |
|---|---|---|---|---|---|
| transformer-from-zero | vectors | available | passed | 2026-08-02: the Codex in-app browser ran both English NumPy cells, repaired the first independent reshape contract, and completed the practice deck to 3/3; Korean 390×844 reproduced a wrong Shape Detective prediction, recovered, and reset to 0/3. Both viewports had zero console warnings/errors; mobile also had zero native selects and page overflow. All four chapter E2E flows now fail on unexpected console errors. Historical 2026-07-22 and 2026-07-30 passes covered all required/optional labs, both locales, completion, navigation, keyboard, reset, overflow, native-select, and 44×44 contracts. 2026-07-31 Chrome additionally revalidated first-failed-contract promotion and recovery. | Shape Detective 3/3, Axis Builder 3/3, concept checks 5/5; learner also ran both Python cells, recovered from NameError, predicted reshape/broadcast failures, and compared aligned, perpendicular, opposite, and zero vectors, then independently inferred reshape across two element counts, repaired the first incompatible broadcast axis, and transferred dot-product shape to two unseen Q/K fixtures |
| transformer-from-zero | optimization | available | passed | 2026-08-02: the Codex in-app browser observed an English divergent trace, repaired only the learning rate to reach steady convergence, ran the NumPy trace, and recovered the intended finite-difference gradient failure by repairing the learner-owned MSE line. Korean 390×844 repeated the divergent-to-convergent repair with zero console warnings/errors, native selects, or page overflow. Both chapter E2E flows now fail on unexpected console errors. Historical 2026-07-22 and 2026-07-30 passes covered the required lab, optional debugger, both NumPy cells, concept retry, three independent challenges, both locales, keyboard, reset, navigation, overflow, native-select, and 44×44 contracts. | Predicted a misleading first step and divergent full trace, repaired only η to converge, diagnosed uphill/stop/overshoot cases to 4/4, repaired the MSE gradient against a central finite-difference probe, recovered five concept errors to 5/5, then independently reproduced both parameter updates on two fixtures, diagnosed a correct-sign overshoot, and transferred the update to a new curvature on two unseen fixtures |
| transformer-from-zero | neural-networks | available | passed | 2026-08-02: the Codex in-app browser recovered the English single-line XOR prediction to 3/4, assembled OR/NAND hidden features to reach XOR 4/4, restored both hidden-backprop factors while reducing BCE 0.329→0.315, ran both NumPy cells and repaired the missing activation to reach 4/4 at mean BCE 0.022529, then promoted and repaired the first independent output-signal contract. Korean 390×844 rebuilt XOR to 4/4; all 95 visible enabled lesson targets met 44×44, with zero console warnings/errors, native selects, or page overflow. All three chapter E2E flows now fail on unexpected console errors. Historical 2026-07-22 and 2026-07-30 passes covered both required labs, debugger incidents, concept retries, all three independent challenges, both locales, keyboard, reset, navigation, overflow, native-select, and 44×44 contracts. | Predicted the single-line XOR limit, assembled a causally necessary OR/NAND hidden representation at 4/4, traced a loss-reducing hidden-layer backprop step, repaired a missing NumPy activation, restored four network contracts, and recovered five concept errors to 5/5, then independently reproduced the BCE output signal on two fresh neurons, verified the complete hidden path against central differences, and transferred two XOR-logit fixtures to XNOR |
| transformer-from-zero | training | available | passed | 2026-07-22: local content-preview, KO/EN, 1280 and 390×844; 32 direct-choice options, both order presets, four Adam updates, six parameter cells, two NumPy cells, four debugger incidents, five concept retries, reset/toggle/feedback controls, Enter/Space, and neural-networks←training→embeddings navigation exercised; 0 console errors, overflow, native selects, or enabled mobile targets below 44×44 after repair. 2026-07-30: added and rechecked a three-level independent training practice deck at 1280×720 and 390×844; deliberate class-gradient-sum failure→retry, English 3/3, Korean duplicated-batch invariance, all-state and current resets, Enter/Space navigation, focus recovery, unchanged disabled completion preview, 0 console warnings/errors, 0 overflow, 0 native select, and 0 enabled practice buttons below 44px | Predicted and observed a local CE decrease with temporary full CE increase, completed the tail batch, traced persistent Adam moments through a parameter update, recovered full CE from 0.3010 to 0.2254, repaired class-axis Softmax, and distinguished validation/Dropout contracts, then independently reproduced fused Softmax-CE gradients on two class rows, preserved mean parameter gradients across B=2→4 and B=3→6 duplication, and transferred fresh gradients through persistent Adam state at t=4 and t=6 |
| transformer-from-zero | embeddings | available | passed | 2026-07-22: local content-preview, KO/EN, 1280 and 390×844; tokenizer presets and modes, prediction/error/recovery paths, all token positions, 11 stable-row choices, both NumPy cells, 12 debugger repairs, 15 concept options, resets/toggles/feedback, Enter/Space, and training←embeddings→sequences navigation exercised; 0 console errors, overflow, native selects, or effective mobile targets below 44×44. 2026-07-30: added and rechecked a three-level independent Embeddings practice deck at 1280×720 and 390×844; deliberate shape/first-row failure→retry, English 3/3, Korean partial/full scatter-add cancellation, all-state and current resets, Enter/Space navigation, focus recovery, unchanged disabled completion preview, and a chapter-scoped 566→390px mobile overflow repair; 0 console warnings/errors, final overflow, native select, or enabled practice buttons below 44px | Predicted tokenizer shape, proved one-hot lookup equivalence, observed repeated row 2 move exactly 2× row 5 while row 4 stayed stable, repaired repeated-index scatter-add to [0.4,-0.2], restored four embedding contracts, and recovered five concept errors to 5/5, then independently reproduced direct lookup on two new ID sequences, distinguished partial from complete repeated-row gradient cancellation, and transferred two unseen-word fixtures through shared `[UNK]` row 1 |
| transformer-from-zero | sequences | available | passed | 2026-07-22: local content-preview, KO/EN, 1280 and 390×844; all three sequence presets, RNN/LSTM prediction and timestep controls, both NumPy cells, 12 debugger repairs, 15 concept choices, failure/retry/reset paths, Enter/Space, and embeddings←sequences→attention preview navigation exercised; 0 console errors, overflow, native selects, or effective mobile targets below 44×44 after repair. 2026-07-30: added and rechecked a three-level independent Sequences practice deck at 1280×720 and 390×844; deliberate final-only/input-only failure→retry, English 3/3, Korean temporal-gradient agreement, all-state and current resets, Enter/Space navigation, focus recovery, unchanged disabled completion preview, and a chapter-scoped 417→390px mobile overflow repair; 0 console warnings/errors, final overflow, native select, or enabled practice buttons below 44px | Observed long-gap RNN gradient fall to 0.0015 at gain 0.4 versus LSTM cell 0.2778 and hidden 0.1960, reversed the final-state direction, matched the temporal finite-difference gradient at 0.005453, restored four sequence contracts, and recovered five concept errors to 5/5, then independently reproduced shared recurrence on T=3/T=4 fixtures, matched x0→hT gradients across three/four recurrent edges, and transferred carry/write/reveal rules across two fresh gate fixtures |
| transformer-from-zero | attention | available | passed | 2026-07-22: local content-preview, KO/EN, 1280 and 390×844; all three query presets, predictions, five routing stages, three contribution rows, three value-only counterfactual choices, both NumPy cells, 12 debugger repairs, 15 concept choices, explicit blank-query failure/recovery, resets, Enter/Space, and sequences←attention→self-attention preview navigation exercised; 0 console errors, overflow, native selects, or effective mobile targets below 44×44 after repair. 2026-07-30: added and rechecked a three-level independent Attention practice deck at 1280×720 and 390×844; deliberate key-space context failure→retry, English 3/3, Korean paired-row invariance, all-state and current resets, Enter/Space navigation, focus recovery, unchanged disabled completion preview, and a chapter-scoped 367→342px internal H2 overflow repair; 0 console warnings/errors, final overflow, native select, or enabled practice buttons below 44px | Routed who/where/what queries to subject/place/action with per-query weights summing to one, explained αV and value-only invariance, repaired context from [3,2] to [3,3], restored four routing contracts, and recovered five concept errors to 5/5, then independently reproduced value-space contexts on two fresh memories, preserved context and semantic top row under two paired K/V/label permutations, and kept weights plus contexts invariant under extreme positive and negative common score shifts |
| transformer-from-zero | self-attention | available | passed | 2026-07-22: local content-preview, KO/EN, 1280 and 390×844; all five challenge presets and predictions, five trace stages, required numeric inspections, both NumPy cells, 13 debugger repairs, 15 concept choices, failure/recovery/reset paths, Enter/Space, and attention←self-attention→transformer-block navigation exercised; 0 console errors, overflow, native selects, or enabled mobile targets below 44×44 after repair. 2026-07-30: added and rechecked a three-level independent Self-Attention practice deck at 1280×720 and 390×844; deliberate keys-only permutation failure→retry, English 3/3, Korean duplicate-row equality, all-state and current resets, Enter/Space navigation, focus recovery, unchanged disabled completion preview, and a chapter-scoped 479→342px internal H2 overflow repair; 0 console warnings/errors, final relevant overflow, native select, or enabled practice buttons below 44px | Distinguished Q/K/V projections from one X, observed sqrt(d_h) soften scores without changing the top key, verified zero future/padding mass and row renormalization, exposed padding-key weight 0.402 without activating its query, preserved [T,4] through two-head concat, restored four contracts, and recovered five concept errors to 5/5, then independently reproduced non-causal row permutation on two fixtures, proved identical rows keep identical contexts without positional or visibility differences, and transferred permutation semantics by jointly relabeling both causal mask axes |
| transformer-from-zero | transformer-block | available | passed | 2026-07-22: local content-preview, KO/EN, 1280 and 390×844; all five challenge presets and 15 predictions, five block stages and required matrix cells, numeric failure/recovery, both NumPy cells, 13 debugger repairs, 15 concept choices, all reset controls, Enter/Space, and self-attention←transformer-block→mini-transformer navigation exercised; 0 console errors, overflow, native selects, or enabled mobile targets below 44×44 after repair. 2026-07-30: EN 1280×720 and KO 390×844 optional independent practice exercised across wrong/correct retries, three direct challenges, Enter/Space, current/all reset, focus restoration, and disabled completion preview; 0 console warnings/errors, native selects, practice overflow, or enabled visible practice targets below 44px; existing long KaTeX formula contained to intentional internal scroll with document 390/390 and article/section 358/358 | Assembled E+P, feature-axis pre-LayerNorm, both residuals, and a shared row-wise ReLU FFN while preserving [4,4]; inspected x₀=0.841, LN(x₀)=-0.333, x₁=0.612, FFN=0.227, and final y=1.883 target cells, restored four contracts, and recovered five concept errors to 5/5. Independently reproduced both residual ledgers, separated normalized-branch shift invariance from identity-path transport, and handed y₁ into block₂ without reinjecting position; wrong second-skip source failed at 0.350000/0.450000 before zero-error recovery, and both shift and two-block transfer fixtures reached zero error |
| transformer-from-zero | mini-transformer | available | passed | 2026-07-22: local content-preview, KO/EN, 1280 and 390×844; all five challenge presets and 15 predictions, five stages and required numeric cells, generation-setting failure/recovery, both NumPy cells, 14 debugger repairs, 15 concept choices, reset controls, Enter/Space, and transformer-block←mini-transformer final navigation exercised; 0 console errors, overflow, native selects, or enabled non-matrix mobile targets below 44×44 after repair. 2026-07-30: EN 1280×720 and KO 390×844 optional independent practice exercised across deliberate wrong/correct retries, three direct challenges, Enter/Space, current/all reset, focus restoration, and unchanged disabled completion preview; 0 console warnings/errors, native selects, practice overflow, or enabled visible practice targets below 44px; repaired an existing Korean debugger H2 from 377px overflow to its 342px content width with document 390/390 and article 358/358. 2026-07-31 Chrome: KO 390×844 preview late fixture failure promoted the first failed row comparison with `먼저 고칠 계약`; changing `short[t] ↔ long[-1]` to `short[t] ↔ long[t]` passed 3/3 checks at progress 1/3, with 0 console warnings/errors, native selects, page/practice overflow, or sub-44px enabled targets. | Closed token IDs→E+P→causal block→final norm→[T,V] logits→shifted CE/update→autoregressive decode; inspected BOS ID 0, E+P 0.841, future weight 0, target-token probability 0.382, and generated-dot probability 0.340, lowered LM-head loss 1.656→1.553, repaired prefix lengths 3→4→5→6→7, restored four contracts, and recovered five concept errors to 5/5. Independently proved appended suffixes leave matching causal-prefix logits unchanged after wrong-row errors `2.731864`/`1.808742`, separated all-row teacher-forced loss from last-row generation with tokens 2/1, and matched incremental KV-cache attention to full causal contexts at zero error including `[0.454352, 0.391821]` and `[0.322331, 0.536958]` |
| linux-systems | shell-and-filesystem | available | passed | 2026-08-01: public KO at 1280 and preview EN at 390×844; current five-question failure/retry/completion flow, 0 console errors, overflow, native selects, or undersized mobile core shell controls; in-app keyboard injection did not dispatch Enter, so that assertion remains source-level | Five deterministic commands reached 5/5; file creation and permission failure were visible beside the command transcript |
| linux-systems | boot-to-shell | available | passed | 2026-08-01: current direct-button contract revalidated in the in-app browser at KO 1280×720 and EN 390×844; wrong/correct boot predictions, 4/4 recovery, wrong diagnosis and all four repairs, reset, zero console errors/native selects/overflow, and all 70 enabled visible mobile lesson targets at least 44×44. The in-app keyboard injector did not dispatch Enter, so that existing assertion remains source-level. Historical 2026-07-22 coverage also exercised the optional v86 lifecycle and six guest commands. | Repaired two distinct boot contracts to 4/4, diagnosed firmware→kernel, rootfs, init, and shell boundaries to 4/4 from last-good markers, recovered five concept errors to 5/5, then booted the isolated Buildroot guest and observed kernel identity, command line, mounts, PID 1/shell process state, and root directory |
| linux-systems | processes-and-signals | available | passed | 2026-08-01: current direct-button contract revalidated in the in-app browser at KO 1280×720 and EN 390×844; wrong/correct fork prediction, seven lifecycle checks, all four incident chains with explicit failure, focused concept retry, and three completion outputs. Mobile exposed 67 visible enabled lesson targets with zero sub-44px targets, native selects, overflow, or console errors. The in-app keyboard injector did not dispatch Enter, so that existing assertion remains source-level. Historical 2026-07-23 coverage also exercised every incident choice, all concept wrong/retry paths, navigation, and Enter/Space. | Created two children while preserving PID across exec, observed round-robin output split between terminal and out.log, excluded and resumed a stopped PID, distinguished sleeping/stopped/zombie recovery, collected status 0/137/143 with waitpid, and recovered five concept errors to 5/5 |
| linux-systems | users-and-permissions | available | passed | 2026-08-01: current direct-button contract revalidated in the in-app browser at KO 1280×720 and EN 390×844; wrong access prediction, no-op/symbolic/octal chmod, four policy checks, chmod 777 overgrant, all four incident repairs, focused concept retry, global reset, and three completion outputs. Mobile exposed 61 visible enabled lesson targets with zero sub-44px targets, native selects, overflow, untranslated strings, or console errors. Historical 2026-07-23 coverage also exercised every preset and choice, individual reset, Enter/Space, disclosures, focus recovery, and navigation. | Traced the first EACCES at missing directory x, assembled exact directory 0750 and file 0640 policy, preserved required denials, rejected chmod 777 and twelve incomplete incident repairs, solved four least-privilege incidents, and recovered five concept errors to 5/5 |
| linux-systems | memory-and-virtual-addresses | available | passed | 2026-08-01: current direct-button contract revalidated in the in-app browser at KO 1280×720 and EN 390×844; wrong/correct translation prediction, five lab evidence checks, wrong/correct physical-address calculation, four incident repairs, focused concept retry, resets, and three completion outputs. Mobile exposed 57 visible enabled lesson targets with zero sub-44px targets, native selects, overflow, untranslated strings, or console errors. Historical 2026-07-23 coverage also exercised every preset and choice, invalid inputs, all outcome types, Enter/Space, disclosures, focus recovery, and navigation. | Preserved the page offset through per-process PTE translation, proved parent/child COW isolation and demand allocation, separated TLB miss from page fault and mapping from residency, repaired four numeric/state incidents, and recovered five concept errors to 5/5 |
| linux-systems | storage-and-filesystems | available | passed | 2026-08-01: current direct-button contract revalidated in the in-app browser at KO desktop and EN 390×844; wrong/correct block prediction, five required evidence checks, four incident diagnoses, focused concept retry, resets, and three completion outputs. Mobile had zero sub-44px visible enabled lesson targets, native selects, overflow, untranslated strings, or console errors. Historical 2026-07-23 coverage also exercised every preset and choice, Enter/Space, disclosures, focus recovery, and navigation. | Resolved `/srv/data/report.bin` through the mounted datafs to inode 17 and device byte 0x2c340, proved hard-link survival after unlink, observed dirty `candidate-v2` revert to `draft`, preserved fsynced `durable-v3`, distinguished inode from block exhaustion, repaired deleted-open lifetime and same-parent write→fsync→rename→directory-fsync, and recovered five concept errors to 5/5 |
| linux-systems | networking-from-a-packet | available | passed | 2026-08-01: current direct-button contract revalidated in the in-app browser at KO desktop and EN 390×844; wrong/correct route and TCP predictions, complete causal packet journey through recv, four incident repairs with one failure, focused concept retry, resets, and three completion outputs. Mobile exposed 90 visible lesson targets with zero sub-44px targets, native selects, overflow, untranslated strings, or console errors. Historical 2026-07-23 coverage also exercised every preset and choice, Enter/Space, disclosures, focus recovery, and navigation. | Kept fd local, selected /24 direct and /0 gateway paths, preserved endpoint IP behind the next-hop MAC, segmented 3000 B as 1460/1460/80, held cumulative ACK at 2461 across loss, retransmitted seq 2461 to ACK 4001, distinguished listener fd 3 from accepted fd 5 and TCP ACK from application recv, repaired four incidents, and recovered five concept errors to 5/5 |
| linux-systems | assemble-a-tiny-linux | available | passed | 2026-08-01: current direct-button contract revalidated in the in-app browser at KO desktop and EN 390×844; wrong/correct prediction, ordered init-mode→return-route→report-mode repairs, all five final probes, four incident repairs with one failure, focused concept retry, resets, and three completion outputs. Mobile exposed 109 visible lesson targets with zero sub-44px targets, native selects, overflow, untranslated strings, or console errors. Historical 2026-07-23 coverage also exercised every practice choice, Enter/Space, disclosures, navigation, and the optional v86 guest plus five commands. | Separated kernel/rootfs artifacts from boot-created state, stopped at the first failed contract, recovered `/init` execution, SYN-ACK return routing, and group-only report read, proved rootfs/PID 1/listener/report/remote-recv boundaries independently, repaired four cross-layer incidents, recovered five concept errors to 5/5, and compared the deterministic model with real guest kernel, mount, process, and command-line markers without claiming a custom build or external network path |
| linux-networking | interfaces-addresses-and-loopback | available | passed | 2026-07-23: local content-preview, KO/EN, 1280 and 390×844; both wrong and correct delivery-boundary predictions, all six interface/loopback phases, Home/End phase keys, all sixteen incident repair choices with failure/retry, incident reset/replay, fifteen concept choices with Enter/Space, lab reset/focus recovery, draft/public boundary, transfer and prerequisite/next/catalog navigation exercised; repaired reset focus falling to the document body; 0 console errors, overflow, or native selects, and 0 enabled mobile targets below 44px | Distinguished existence, admin state, carrier, address, name resolution, route, and delivery boundary; proved admin-UP Ethernet can remain operationally down, localhost can still resolve while loopback is unusable, repaired four single-boundary incidents, recovered five concept errors to 5/5, and checked all three chapter completion outputs |
| linux-networking | subnets-neighbors-and-gateways | available | passed | 2026-07-23: local content-preview, KO/EN, 1280 and 390×844; both wrong next-hop predictions and the correct gateway, all six path phases, every repair choice in four incidents, reset/replay, fifteen concept choices with Enter/Space, all seven section links, transfer, previous/next, catalog, and draft/public-boundary navigation exercised; repaired the reset control from 42px desktop width and 40px mobile height to the 44px contract; 0 console errors, overflow, native selects, or enabled mobile targets below 44px after repair | Derived 10.20.0.0/24, distinguished direct peer from default-gateway next hop, preserved the remote IP while addressing the first-hop frame to the gateway MAC, observed neighbor resolution and no-route failure, repaired four one-boundary incidents, recovered five concept errors to 5/5, and checked all three chapter completion outputs |
| linux-networking | routes-and-packet-paths | available | passed | 2026-07-23: local content-preview, KO/EN, 1280 and 390×844; both wrong route-decision predictions and the correct longest prefix, all six route/frame/TTL phases, every repair choice in three incidents, deterministic incident reset/replay, fifteen concept choices with Enter/Space, all seven section links, transfer, previous/next, catalog, and draft/public-boundary navigation exercised; added the missing shared incident reset and preserved focus on it; 0 console errors, page overflow, native selects, or enabled mobile targets below 44px | Selected /24 before default and used metric only for equal-length candidates, kept the final IP destination while rebuilding each link frame, observed TTL 1 become 0 with ICMP Time Exceeded, repaired three first-failed boundaries, recovered five concept errors to 5/5, and checked all three completion outputs |
| linux-networking | sockets-ports-and-tcp | available | passed | 2026-07-26: local content-preview, KO/EN, 1280 and 390×844; both wrong application-delivery predictions and correct recv boundary, all six listener/connect/accept/send/ACK/recv states, every repair choice in three incidents, reset/replay, all fifteen concept choices with wrong/retry and Enter/Space, seven section links, Infrastructure transfer, previous/next, catalog, and draft/public-boundary navigation exercised; corrected the locked initial progress from misleading 1/6 to 0/6; 0 console errors, page overflow, native selects, or enabled mobile targets below 44px | Kept listener fd 3 separate from accepted fd 4, identified one flow by its 4-tuple, observed send bytes reach the peer-kernel queue, bounded ACK evidence to kernel receipt, used recv as application-delivery proof, repaired listener scope, stalled recv loop, and EADDRINUSE minimally, then recovered five concept errors to 5/5 and checked all three completion outputs |
| linux-networking | dns-and-service-reachability | available | passed | 2026-07-26: local content-preview, KO/EN, 1280 and 390×844; both wrong service-boundary predictions and correct route/connect boundary, all six cache-miss/DNS-answer/cache-hit/route/TCP/response states, every repair choice in three incidents, reset/replay with focus retention, all fifteen concept choices with five wrong-answer recoveries and Enter/Space, seven section links, Infrastructure transfer, previous/next, catalog, and draft/public-boundary navigation exercised; corrected the chapter compass from compare/tune to predict/repair and exposed response body beside HTTP status; 0 console errors, page overflow, native selects, or enabled mobile targets below 44px | Distinguished resolver, authoritative answer, fresh cache, and RFC 8767 serve-stale exception; followed name → route → TCP → application boundaries, observed `status=ok` beside HTTP 200, repaired NXDOMAIN, one unexpired stale cache entry, and a missing listener minimally, then recovered five concept errors to 5/5 and checked all three completion outputs |
| linux-networking | diagnose-a-linux-network | available | passed | 2026-07-26: local content-preview, KO/EN, 1280 and 390×844; both wrong observation-point predictions and the correct scoped prediction, all six link/address→route/neighbor→packet→listener→name→response states, all twelve incident choices with immediate failure/retry, reset/replay, all fifteen concept choices with five wrong-answer recoveries, seven section links, Infrastructure transfer, previous/next, audit catalog, and draft/public-boundary navigation exercised; made the final `curl` output expose the HTTP status supporting its result card; 0 console errors, horizontal overflow, native selects, or enabled mobile targets below 44px | Located the first failed boundary instead of changing unrelated state, matched `ip route get` egress with the tcpdump observation point, separated SYN/SYN-ACK, listener, name, body, and HTTP evidence, repaired four multi-boundary incidents minimally, recovered five concept errors to 5/5, and checked all three completion outputs |
| infrastructure-design | network-namespaces-and-boundaries | available | passed | 2026-08-01: current direct-button contract revalidated in the in-app browser at KO 1280×720 and EN 390×844; all four ownership phases, 18 placement transitions, both loopback switches, every prediction, incident distractor/repair, all 15 concept choices, individual/global resets, nine anchors, and language switching exercised. Korean desktop reached all three completion outputs; mobile exposed 53 enabled visible targets with zero sub-44px targets, native selects, untranslated strings, page overflow, or console errors. Historical 2026-07-26 coverage also verified navigation and draft/public boundaries. | Distinguished thread namespace membership from socket ownership, proved localhost and wildcard listeners stay namespace-local, built app/data-local health while host and cross-service localhost failed, repaired four boundary incidents including recreation after `setns`, recovered five concept errors to 5/5, and checked all three completion outputs |
| infrastructure-design | veth-bridges-and-routing | available | passed | 2026-08-02: current direct-button contract revalidated in the in-app browser at KO 1280×720 and EN 390×844; every bridge/router placement, link, address, route, forwarding, listener, prediction, incident distractor/repair, all 15 concept choices, resets, nine anchors, help, language switching, and public 404 boundary exercised. Korean desktop reached four completion outputs; both completed viewports exposed 65 enabled controls with zero sub-44px targets, native selects, untranslated strings, or component/page overflow. No UI mutation was warranted. Historical 2026-07-27 coverage also verified navigation, catalog, and draft/public boundaries. | Built and verified both same-subnet bridge and two-subnet router round trips, distinguished two veth endpoint owners, bridge ports, on-link gateways, forwarding, forward and return routes, repaired four first-failed topology invariants, recovered a focused ownership error and all five concepts to 5/5, and checked all four completion outputs |
| infrastructure-design | egress-nat-and-conntrack | available | passed | 2026-08-01: current direct-button contract revalidated in the in-app browser at KO 1280×720 and EN 390×844; every switch, NAT option, prediction, both modes, incident distractor/repair, all 15 concept choices, resets, nine anchors, and language switching exercised. Repaired a completed-state container-width mismatch that clipped nested controls by up to 130px. Mobile exposed 51 enabled visible targets with zero sub-44px targets, native selects, overflow, untranslated strings, or console errors. | Built and verified fixed SNAT and dynamic MASQUERADE round trips, separated route/forwarding/translation failures from reply-path failures, observed `NEW` before bidirectional traffic and `ESTABLISHED` only after the reply, repaired four first-failed NAT invariants, recovered five concept errors to 5/5, and checked all four completion outputs |
| infrastructure-design | network-policy-and-firewalls | available | passed | 2026-08-01: current direct-button contract revalidated in the in-app browser at KO 1280×720 and EN 390×844; both modes, every hook/default/state/scope/order/prediction alternative, failed/repaired probes, all incident distractors/repairs, resets, 15 concept options, ten anchors, and locale switching exercised. Korean desktop reached four completion outputs; completed mobile had 46 enabled controls with zero sub-44px targets, native selects, untranslated strings, component/page overflow, or console errors. Historical 2026-07-27 coverage also verified navigation, catalog, draft/public boundaries, and the ESTABLISHED-only reply correction. | Built default-deny forward and input policies to 2/2, distinguished hook and ordered-rule boundaries, allowed only tracked return traffic, repaired four incidents to 4/4, recovered five concept errors to 5/5, and checked all four completion outputs |
| infrastructure-design | service-discovery-and-load-balancing | available | passed | 2026-08-01: current direct-button contract revalidated in the in-app browser at KO 1280×720 and EN 390×844; every DNS policy, VIP-retirement/listener state, membership, algorithm, affinity, prediction, incident distractor/repair, all 15 concept choices, resets, ten anchors, help, language switching, and public 404 boundary exercised. Repaired a hidden 691/542px desktop service-map mismatch by responding to the visualization's actual inline size. Korean desktop reached four completion outputs; mobile exposed 48 enabled controls with zero sub-44px targets, native selects, untranslated strings, component/page overflow, or console errors. Historical 2026-07-27 coverage also verified navigation and catalog boundaries. | Distinguished name, TTL cache, VIP, healthy membership, and affinity boundaries; completed DNS and load-balancer modes to 2/2, repaired four incidents to 4/4, recovered a focused TTL error and all five concepts to 5/5, and explained the RFC 8767 serve-stale exception |
| infrastructure-design | availability-and-failure-domains | available | passed | 2026-08-01: current direct-button contract revalidated in the in-app browser at KO 1280×720 and EN 390×844; every placement, prediction, 20/90-second recovery budget, incident distractor/repair, all 15 concept choices, resets, nine anchors, and language switching exercised. A reproduced 4px desktop overflow from `recommendations` was repaired without clipping. Mobile exposed 43 enabled visible lesson targets with zero sub-44px targets, native selects, overflow, untranslated strings, or console errors. | Placed replicas across independent domains, observed failover loss and availability update immediately at 20s versus 90s, recovered both failure modes to 2/2, repaired four incidents to 4/4, and recovered five concept errors to 5/5 |
| infrastructure-design | network-observability-and-capacity | available | passed | 2026-08-01: current direct-button contract revalidated in the in-app browser at KO 1280×720 and EN 390×844; all 28 evidence alternatives, every capacity prediction/plan, scenario failure/recovery, all incident distractors/repairs, resets, 15 concept options, ten anchors, and locale switching exercised. Repaired four clipped packet-path columns to follow the 359px visualization container. Korean desktop reached six completion outputs; mobile had zero sub-44px targets, native selects, untranslated strings, component/page overflow, or console errors. Historical 2026-07-27 coverage also verified navigation and draft/public boundaries. | Aligned four claims with bounded evidence, completed bandwidth/queue/connection capacity checks to 4/4, distinguished utilization, saturation, errors, and namespace scope, repaired four incidents to 4/4, and recovered five concept errors to 5/5 |
| infrastructure-design | assemble-a-namespace-platform | available | passed | 2026-08-01: current direct-button contract revalidated in the in-app browser at KO desktop and EN 390×844; evidence 7/7, four scenarios, an incident failure then all four repairs, a focused concept retry, and seven completion outputs passed. Mobile exposed 76 visible enabled lesson targets with zero sub-44px targets, native selects, overflow, untranslated strings, or console errors. Historical 2026-07-27 coverage also exercised all 33 blueprint choices, resets, anchors, navigation, and draft/public boundaries. | Ran seven evidence evaluators, assembled a verifiable 12/12 namespace platform, passed normal/private-egress/zone-failure/900-rps scenarios to 5/5, repaired four incidents to 4/4, recovered five concept errors to 5/5, and checked the decision record against the observed evidence |
| system-architecture | requirements-and-quality-attributes | planned | planned | No chapter renderer | Planned trade-off simulator output |
| system-architecture | components-and-request-flows | planned | planned | No chapter renderer | Planned architecture-canvas output |
| system-architecture | data-ownership-and-source-of-truth | planned | planned | No chapter renderer | Planned ownership/conflict output |
| system-architecture | sync-async-and-idempotency | planned | planned | No chapter renderer | Planned retry/duplicate output |
| system-architecture | caching-and-consistency | planned | planned | No chapter renderer | Planned latency/staleness output |
| system-architecture | capacity-scaling-and-partitioning | planned | planned | No chapter renderer | Planned bottleneck/headroom output |
| system-architecture | reliability-observability-and-slos | planned | planned | No chapter renderer | Planned fault/error-budget output |
| system-architecture | design-and-review-a-system | planned | planned | No chapter renderer | Planned decision/risk review output |

## Baseline contract

- Implemented quality report on 2026-07-22: 32 chapters, 0 structural issues,
  0 improvement targets, and 0 native select definitions.
- Local audit index on 2026-07-22: 5 curriculum overview links, 32 implemented
  chapter links, 8 planned labels, and 0 links on planned chapters. Desktop and
  390×844 both had 0 overflow and 0 native selects; mobile had 40 controls and
  0 controls below 44×44. English navigation and the localized return path were
  exercised in the in-app browser.
- Local draft access must require a Vite development build, explicit
  `content-preview` mode, and a loopback host. Normal `npm run dev` must remain
  fail-closed for unauthenticated draft URLs.
- A row passes only after actual interaction, visible result inspection,
  language switching, console/error review, and 390×844 overflow/touch-target
  checks.
